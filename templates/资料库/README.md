# 资料库

资料库保存长期稳定的背景资料，不写具体执行步骤。具体步骤放在 `工作流/`，页面操作放在 `DOM地图/`。

## 默认文件

- `固定规则.md`: 长期规则、命名规则、字段解释。
- `业务背景.md`: 业务上下文和常见对象。
- `网站入口.md`: 常用网络链接地图——系统入口、常用链接、表格入口、对应 DOM 地图。任务专属链接写进对应 `workflow.md`。
- `账号与权限说明.md`: 权限前提和账号注意事项。
