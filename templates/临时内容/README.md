# 临时内容

`临时内容/` 是本次工作的草稿、中间文件和验证区。保存执行过程中产生的临时文件。详细归类和清理边界以 `系统索引/产出归类与命名规范.md` 为准。

- `草稿/`: 未采用版本和草稿。
- `中间文件/`: 转换、导出、处理过程文件。
- `验证文件/`: 临时检查材料，可转入 `output/验证证据/`。

本目录是工作草稿区，默认可清理；需要长期保留的内容必须先按归类规则转入 `output/` 或 `归档/`。彻底不要的内容请移到 `_待删除/` 等脚本清理，不要直接删除。
