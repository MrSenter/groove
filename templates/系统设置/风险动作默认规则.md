# 风险动作默认规则

以下动作默认需要用户确认:

- 提交外部系统
- 删除文件或记录
- 付款
- 报废确认
- 覆盖正式产出
- 写入线上数据
- 对外发送消息

默认先生成草稿、检查清单或预览, 再请求确认。

## 极速模式不豁免

极速模式只减少读取 / 叙述 / 探索性核对。对**不可逆动作**(上表这些), 廉价不变量校验(数量 / 选中数 / 成功回读)和确认**始终保留**。让某不可逆工作流进入极速, 等于用户对"减少检查"的一次预授权, 必须由用户点头。
