# _待删除（待删除回收区）

这是显眼的"待删除回收区"。

## 使用规则

- **agent 判断要丢弃的内容，移到本目录，而不是直接删除。**
- 清理脚本（`工具/scripts/` 下，按你的系统由 agent 生成）按 `系统设置/节奏设置.md` 中的"待删除目录清理周期"自动清空本目录内容。
- `output/`、`归档/`、`执行记录/`、`巡查/` 永不被清理；清理范围仅限本目录 `_待删除/`。

## 本目录会被自动清理

不要把需要保留的内容放到这里。如果只是暂存草稿，请放到 `临时内容/`。
